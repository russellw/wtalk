wtalk provides voice input on Windows, using a local model running on CPU
It does not try to run in the background, splice itself into the operating system or input text directly into other programs
It just runs as a normal Windows program and opens its own edit box for input text
Technologies used:
C++
Win32
Microsoft command line compiler
whisper.cpp
base.en
